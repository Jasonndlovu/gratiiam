import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.comp.gratiiam',
  appName: 'Gratiiam',
  webDir: '../gratiiam',
  bundledWebRuntime: false
};

export default config;
